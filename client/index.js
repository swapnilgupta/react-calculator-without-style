import React from 'react';
import ReactDOM from 'react-dom';

// using the render method, we will mount this node into our DOM (html file) on elements with
// id of 'app'
ReactDOM.render(
    <div> Hello World--swapnil</div>,
    document.getElementById('app')
);